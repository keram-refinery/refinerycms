en:
  refinery:
    resource: File
    plugins:
      resources:
        title: Files
        description: Upload and link to files
    admin:
      resources:
        delete: Remove this file forever
        edit: Edit this file
        form:
          download_current: Download current file
          or: or
          replace: " replace it with this one..."
          maximum_file_size: The maximum file size is %{bytes}.
        resource:
          download: Download this file (%{size})
        actions:
          upload_new: Upload new file
          edit_resource: Edit file
        records:
          no_files_yet: There are no files yet. Click "Upload new file" to add your first file.
        insert:
          existing: Existing File
          new: New File
          no_files: There are no files.
        existing_resource:
          link_to_file: Link to this file
          button_text: Insert
        problem_create_resources: 'These files we was unable create: %{resources}.'
      resources_dialog:
        index:
          existing_resource: Library
          external_resource: Url
          upload_resource: Upload
        no_resources_yet: There are no files yet.
      links_dialog:
        index:
          resources: Files
  activerecord:
    models:
      refinery/resource: File
    errors:
      models:
        refinery/resource:
          blank: You must specify file for upload
          too_big: File should be smaller than %{size} bytes in size
          file_exists: 'File with name %{name} already exists'
          different_file_name: New file can't have different file name
