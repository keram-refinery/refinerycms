<ul id="<%= refinery_dom_id @images %>" class="records image-grid clearfix <%= 'ui-selectable' if dialog? %>">
  <%= render :partial => 'refinery/admin/images/grid_view_image', :collection => @images %>
</ul>
