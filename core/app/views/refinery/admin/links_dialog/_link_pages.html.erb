<div>
  <ul class="records ui-selectable">
  <%= render :partial => 'page_link', :collection => @pages,
             :locals => {
               :child => 0
             } %>
  </ul>
  <%= will_paginate @pages, :params => params.dup.merge(:dialog => 1) %>
</div>
