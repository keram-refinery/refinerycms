require 'refinerycms-core'

module Refinery
  autoload :<%= extension_class_name %>Generator, 'generators/refinery/<%= extension_name %>_generator'

  module <%= namespacing %>
    require 'refinery/<%= extension_name %>/engine'

    class << self
      def root
        @root ||= Pathname.new(File.expand_path('../../../', __FILE__))
      end
    end
  end
end
