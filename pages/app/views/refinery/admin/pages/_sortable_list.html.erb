<%
  config = {
    update_url: refinery.url_for(action: 'update_positions', frontend_locale: Globalize.locale)
  }
%>
<ul class="records sortable-list tree" data-sortable-options="<%= config.to_json %>">
  <%= render partial: 'page', collection: @pages.roots %>
</ul>
